// Re-exported from server — this import is resolved at build time
// when server package is built first via Turborepo dependency graph.
// In dev, TypeScript resolves directly via workspace path.
import type { AppRouter } from "@budnet/server/router";
export type { AppRouter };
